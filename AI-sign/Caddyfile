:80 {
	encode gzip

	# Proxy API requests to the backend service
	handle /api/* {
		reverse_proxy ai-be:8080
	}

	# Serve static files with SPA fallback to index.html
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
